# `resolve_workspace_full_text`

合同名：`resolve_workspace_full_text`（category=`access`）。

`requires_network=True`、`mutates_workspace=True`。
为 active workspace papers 探测全文候选（OpenAlex / Unpaywall / 出版商），但不下载。