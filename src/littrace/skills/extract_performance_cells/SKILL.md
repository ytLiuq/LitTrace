# `extract_performance_cells`

合同名：`extract_performance_cells`（category=`extraction`）。

`quality_requirements=["extraction_verifier"]`，`provenance_outputs=["EvidenceSpan"]`。
会把每个 cell 的 `paper_id / page / table / snippet` 写回 workspace。