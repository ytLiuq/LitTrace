# `build_storyline_from_workspace`

合同名：`build_storyline_from_workspace`（category=`synthesis`，同步）。

纯本地：从 workspace 的 parsed papers + active papers 生成跨论文 `StorylineClaim` 列表。
供 `_run_quality_gates` 的 Storyline Gate 与 `research_writer` 共用。