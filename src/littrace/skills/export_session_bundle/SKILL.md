# `export_session_bundle`

合同名：`export_session_bundle`（category=`export`）。

`provenance_outputs=["ReleaseSnapshot"]`、`quality_requirements=["publication_gate"]`。
通过发布门后，把 session 的论文、artifacts、表格、storyline 打包成 zip 等外部可携带产物。