[project]
name = "littrace-plugin-demo"
version = "0.1.0"
description = "Demo third-party LitTrace MCP plugin (round 13 sample)"
requires-python = ">=3.11"
dependencies = []

[project.entry-points."littrace.mcp_servers"]
vendor_search = "littrace_plugin_demo:register"